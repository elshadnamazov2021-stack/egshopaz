import { createFileRoute, Link } from "@tanstack/react-router";
import { useMemo, useState } from "react";
import { useTranslation } from "react-i18next";
import { Store, MapPin, Search } from "lucide-react";
import { shops } from "@/data/staticStore";
export const Route = createFileRoute("/shops")({ head: () => ({ meta: [{ title: "Mağazalar — EG Shop" }] }), component: ShopsPage });
function ShopsPage() { const { t } = useTranslation(); const [q, setQ] = useState(""); const filtered = useMemo(() => { const s = q.trim().toLowerCase(); return !s ? shops : shops.filter((sh) => sh.shop_name.toLowerCase().includes(s) || sh.shop_city.toLowerCase().includes(s) || sh.shop_description.toLowerCase().includes(s)); }, [q]); return <div className="container mx-auto px-4 py-6 space-y-5"><div className="flex items-center gap-3"><div className="w-12 h-12 rounded-2xl bg-gradient-to-br from-primary to-blue-500 flex items-center justify-center shadow-card"><Store className="h-6 w-6 text-white" /></div><div className="flex-1"><h1 className="text-2xl md:text-3xl font-black">{t("shops.title")}</h1><p className="text-sm text-muted-foreground">{t("shops.count", { count: filtered.length })}</p></div></div><div className="relative"><Search className="h-4 w-4 absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" /><input value={q} onChange={(e) => setQ(e.target.value)} placeholder={t("shops.searchPlaceholder")} className="w-full pl-10 pr-4 py-3 rounded-xl bg-secondary border border-border focus:border-primary outline-none" /></div>{filtered.length === 0 ? <div className="text-center py-16 bg-secondary/40 rounded-2xl"><Store className="h-10 w-10 mx-auto text-muted-foreground mb-3" /><p className="text-muted-foreground">{t("shops.notFound")}</p></div> : <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">{filtered.map((p) => <div key={p.id} className="bg-card border border-border rounded-2xl overflow-hidden hover:shadow-elegant transition"><Link to="/shop/$id" params={{ id: p.id }} className="block"><div className="aspect-[16/7] bg-gradient-brand" /><div className="px-4 pt-4 flex items-start gap-3 -mt-8"><div className="w-14 h-14 rounded-2xl bg-card border-2 border-card shadow overflow-hidden shrink-0 flex items-center justify-center"><Store className="h-6 w-6 text-muted-foreground" /></div><div className="min-w-0 flex-1 pt-7"><div className="font-bold line-clamp-1">{p.shop_name}</div><div className="text-xs text-muted-foreground flex items-center gap-1"><MapPin className="h-3 w-3" />{p.shop_city}</div></div></div><p className="px-4 py-3 text-xs text-muted-foreground line-clamp-2">{p.shop_description}</p></Link></div>)}</div>}</div>; }

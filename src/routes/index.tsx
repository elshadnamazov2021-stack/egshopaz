import { createFileRoute, Link } from "@tanstack/react-router";
import { useTranslation } from "react-i18next";
import { ProductCard } from "@/components/ProductCard";
import { SponsoredProducts } from "@/components/SponsoredProducts";
import { SellerBanners } from "@/components/SellerBanners";
import { HomeCategoryBrowser } from "@/components/HomeCategoryBrowser";
import { FeaturedShops } from "@/components/FeaturedShops";
import { products, promoCodes } from "@/data/staticStore";
import { Tag, Flame, TicketPercent, TrendingUp, Copy, Gift } from "lucide-react";
import { toast } from "sonner";
import i18n from "@/i18n";

export const Route = createFileRoute("/")({
  head: () => ({ meta: [{ title: i18n.t("seo.homeTitle") }, { name: "description", content: i18n.t("seo.homeDescription") }] }),
  component: Index,
});

function Index() {
  const { t } = useTranslation();
  const allProducts = products;
  const discounted = products.filter((p) => p.old_price && p.old_price > p.price).slice(0, 5);
  const trending = [...products].sort((a, b) => b.reviews_count - a.reviews_count).slice(0, 6);
  const giveaways = products.slice(0, 4);
  const copyCode = (code: string) => { navigator.clipboard?.writeText(code); toast.success(t("home.codeCopied", { code })); };
  return <div className="container mx-auto px-3 md:px-4 py-4 space-y-5 md:space-y-8"><h1 className="sr-only">EG Shop — statik web vitrin</h1><HomeCategoryBrowser /><SellerBanners /><FeaturedShops /><SponsoredProducts limit={6} />{giveaways.length > 0 && <section className="rounded-2xl md:rounded-3xl bg-gradient-to-br from-amber-400 via-orange-500 to-pink-500 p-4 md:p-8 text-white shadow-elegant relative overflow-hidden"><div className="relative z-10 flex items-end justify-between mb-5"><div><div className="inline-flex items-center gap-2 bg-white/20 backdrop-blur px-3 py-1 rounded-full text-xs font-bold mb-2"><Gift className="h-3.5 w-3.5" /> {t("home.giveawayBadge")}</div><h2 className="text-xl md:text-4xl font-black">{t("home.giveawayTitle")}</h2><p className="text-sm opacity-95 mt-1">{t("home.giveawayDesc")}</p></div></div><div className="relative z-10 grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3 bg-white rounded-xl md:rounded-2xl p-2.5 md:p-3 mobile-product-grid home-product-strip">{giveaways.map((p) => <ProductCard key={p.id} p={p} />)}</div></section>}{discounted.length > 0 && <section className="rounded-2xl md:rounded-3xl bg-gradient-to-br from-discount via-discount to-rose-700 p-4 md:p-8 text-white shadow-elegant"><div className="flex items-center justify-between gap-3 mb-4 md:mb-5"><div><div className="inline-flex items-center gap-2 bg-white/20 backdrop-blur px-3 py-1 rounded-full text-xs font-bold mb-2"><Tag className="h-3.5 w-3.5" /> {t("home.discountBadge")}</div><h2 className="text-xl md:text-4xl font-black leading-tight">{t("home.discountTitle")}</h2></div><Link to="/discover" className="text-sm font-bold hover:underline whitespace-nowrap">{t("home.viewAllArrow")}</Link></div><div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3 bg-white rounded-xl md:rounded-2xl p-2.5 md:p-3 mobile-product-grid home-product-strip">{discounted.map((p) => <ProductCard key={p.id} p={p} />)}</div></section>}{promoCodes.length > 0 && <section><div className="flex items-end justify-between mb-4"><div><div className="inline-flex items-center gap-2 text-success font-bold text-xs uppercase mb-1"><TicketPercent className="h-4 w-4" /> {t("home.promoBadge")}</div><h2 className="text-2xl md:text-3xl font-black">{t("home.promoTitle")}</h2></div></div><div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">{promoCodes.map((p) => <div key={p.id} className="relative bg-gradient-to-r from-success via-emerald-500 to-teal-500 rounded-2xl p-5 text-white shadow-card overflow-hidden"><div className="relative z-10 flex items-center justify-between gap-3"><div><div className="text-3xl font-black mb-1">{p.discount_percent ? `-${p.discount_percent}%` : `-${p.discount_amount} ₼`}</div><div className="text-xs opacity-90">{t("home.promoMinOrder")}: {p.min_order} ₼</div></div><button onClick={() => copyCode(p.code)} className="bg-white text-success px-4 py-2.5 rounded-xl font-black text-sm flex items-center gap-2 hover:scale-105 transition">{p.code} <Copy className="h-3.5 w-3.5" /></button></div></div>)}</div></section>}<section><div className="flex items-end justify-between mb-4"><div className="flex items-center gap-3"><div className="w-12 h-12 rounded-2xl bg-gradient-to-br from-orange-500 to-red-500 flex items-center justify-center shadow-card"><Flame className="h-6 w-6 text-white" /></div><div><div className="text-xs text-muted-foreground font-bold uppercase">{t("home.trendingSubtitle")}</div><h2 className="text-2xl md:text-3xl font-black">{t("home.trendingTitle")}</h2></div></div><Link to="/discover" className="text-sm text-primary font-bold hover:underline">{t("home.viewAllArrow")}</Link></div><div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-3 md:gap-4 mobile-product-grid">{trending.map((p) => <ProductCard key={p.id} p={p} />)}</div></section><section><div className="flex items-end justify-between mb-4"><div className="flex items-center gap-3"><div className="w-12 h-12 rounded-2xl bg-gradient-to-br from-violet-500 to-indigo-500 flex items-center justify-center shadow-card"><TrendingUp className="h-6 w-6 text-white" /></div><h2 className="text-2xl md:text-3xl font-black">{t("home.forYou")}</h2></div><Link to="/catalog" search={{ q: undefined, cat: undefined } as never} className="text-sm text-primary font-bold hover:underline">{t("home.viewAllArrow")}</Link></div><div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-3 md:gap-4 mobile-product-grid">{allProducts.map((p) => <ProductCard key={p.id} p={p} />)}</div></section></div>;
}

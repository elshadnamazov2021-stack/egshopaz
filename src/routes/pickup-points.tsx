import { createFileRoute } from "@tanstack/react-router";
import { useMemo, useState } from "react";
import { pickupPoints } from "@/data/staticStore";
import { MapPin, Clock, Search } from "lucide-react";

export const Route = createFileRoute("/pickup-points")({ head: () => ({ meta: [{ title: "Çatdırış nöqtələri — EG Shop" }] }), component: PickupPointsPage });
function PickupPointsPage() { const [q,setQ]=useState(""); const list=useMemo(()=>pickupPoints.filter(p=>!q || `${p.name} ${p.city} ${p.address} ${p.point_number}`.toLowerCase().includes(q.toLowerCase())),[q]); return <div className="container mx-auto px-4 py-6"><h1 className="text-2xl md:text-3xl font-black mb-4 flex items-center gap-2"><MapPin className="h-6 w-6 text-primary" /> Çatdırış nöqtələri</h1><div className="relative mb-4"><Search className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" /><input value={q} onChange={(e)=>setQ(e.target.value)} placeholder="Axtar" className="w-full pl-10 pr-4 h-11 rounded-lg border border-input bg-background" /></div><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-3">{list.map((p)=><div key={p.id} className="bg-card border border-border rounded-2xl p-4"><div className="font-bold mb-1">#{p.point_number} · {p.name}</div><div className="text-xs text-primary font-semibold mb-2">{p.city}</div><div className="text-sm space-y-1 text-muted-foreground"><div className="flex items-start gap-2"><MapPin className="h-4 w-4 mt-0.5 shrink-0" /> {p.address}</div><div className="flex items-center gap-2"><Clock className="h-4 w-4" /> {p.working_hours}</div></div></div>)}</div></div>; }
